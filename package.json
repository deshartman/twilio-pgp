{
  "name": "pgp-test",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "twilio-run",
    "deploy": "twilio-run deploy"
  },
  "dependencies": {
    "twilio": "^3.56"
  },
  "devDependencies": {
    "twilio-run": "^3.1.1",
    "express": "^4.17.1",
    "express-basic-auth": "^1.2.0"
  },
  "engines": {
    "node": "12"
  }
}
